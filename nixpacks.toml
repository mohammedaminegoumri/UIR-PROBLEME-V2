[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake", "sqlite"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server/server.js"

# ← This is the important line that stops Caddy
[env]
NIXPACKS_SPA_CADDY = "false"
